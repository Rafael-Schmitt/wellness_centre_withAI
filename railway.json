{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn wellness_centre.wsgi:application --bind 0.0.0.0:$PORT"
    }
    
}